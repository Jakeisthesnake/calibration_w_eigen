Todo
- Visualize 3d data from 3 cameras
    - load intrinsics and extrinsics DONE
    - load data and target           DONE?
    - project target onto data using int/ext DONE?
    - Make Frame_data (detected points + timestamp) from loaded points and feed into vizualization DONE
    - Build poses? DONE
    - Make sure all the input args for the vizualization of calculated in main DONE
    - modify vizulaizer to allow on screen modification of int/ext while updating error and vizuals DONE (needs error)
    - Check against synthetic data
    - ALso figure out how to trim projections to what is realistic?
- Generate perfect synthetic 3d data DONE?
- Get frames and timestamps to sync up DONE?
- figureout how to get all points out of the sorting process. DONE
- Figure out python script transforming point to image.DONE
- figure out error computation DONE
- get 2 frames of sym data working DONE
- Hmmm vizualization seems briken. Fix.
- Try loading data set with know good calibration numbers (first against python set) export extrinsics to set as prior for optimization ... and fix? Just to check if things look right on the other side of the pipeline?
- - initialize inter camera extrinisics uses extrinisics estimate from homography
- - then use that initialize inter cam extriniscs to initial target poses
- Check vizualized tranforms based on what the data waht gereated with
  -  check that make frames is doing what it is supposed to be doing
  - why am i seeing frames with no data in vizualization? DONE
  - Now figure out how to show data when there are less than 10 points
  - 10/7 Is it bad if the rotation matirx is the transpose? DONE (yes - it was bad)
  - 10/9 Maybe vizualize each step of optimization? DONE
  - 10/14 Understand optimization... and why it doesn't seem to be happy...
  - 10/14 Mayb optimize for a single frame and then add other frames in?...
  - 10/14 save homography generated priors and then optime for one frame, the 2 etc until it bugs out
    -`10/21 figure out why a single frame doesn't get it dead on
    - 10/27 The hunt is afoot! I am sending different arguments between the two functions.
    - 10/27 Figure out the correct scope to declare those parameters in.
    - 10/27 welp, i figured out that the cam0 pose needed to  be nailed down, but now I need to figure everything else out...
    - 10/29 look at the optimization sequence and see if error goes down as expected.
    - 10/31 Add damping to beginning. Or maybe outlier rejection?
    - 11/6 discard reprojection if the target is out of view... or figure out why it seems to be acceptable? Maybe vizualize the error for each frame?
    - 11/7 track why error isn't displayed correctly DONE
    - 11/10 run the calibration on a few random frames (or better yet, identify the frames that have the lowest average error and use those) and then use that to initialize the full problem.
    - figure out correct indexing - set single_timestamp to be 0 or -1? DONE
    - 11/20 ok, so figure out how to get optimization to work on frame 14 where cam 2 is reasonably initialized and should be able to just scooch its way to 0 error.
    - 11/21 look into the discrepency in how error is calculated between vizualizer and calibration.
    - 11/26 work on frame 18 and base initialization DONE
    - 12/1 figure out how to get intrinsics to converge well. Maybe shuffle a few times betweens optimizing intrinsics and extrinsics?
    - 12/2 isolate frame 6 and get working since you only have to deal with one camera, then frame 1 with all 3 cameras
    - 12/3 may initialize intrinisce with each individual frame as well?
    - 12/8 optimize first frame from initial claibration.
    - 12/9 optimize per image (not per frame) to get camera iintrinsics and extrinsics with respect to each target an use that to get the inter camera data.
    - 12/11 fix vizualization crash
    - 12/16 still try to get a single frame working. It seems that there is a bug that is cause the same data to be shown on all three frames?
    - 12/23 bit of a goose chase - it seemed to calibrate the frame fine but it suggested that there should be more objects points. Reran the python simulation and I think it made new data. So oh well.
    - 12/30 running on one frame with no convergence. Well it converges from cam0 but not the other cams. Net step ask cursor to allow enough leeway to get convergence on the other cams
    - 12/31 hmmm why does cam0 converge but the other two don't? I have added vizualization for the other two, but I haven't yet looked the the results.
    - 1/26 look at the initialization for cam 1 and 2 and maybe enable stored oprimization frames so we can see why they aren't optimizing.
    - 1/27 okok so it looks like the initial calibration is decenet. now I should do a much larger set of calibrations and then compared where the reproject the cameras to be and see if taking an average makes sense.
          though honestly the per frame optimization could be a lot better?
    - 1/28 ok, i need to visualize each frame individually, so I need to break out the synthetic data into frames
    - 2/2 vizualized each frame, now I need to look at the ones that failed compeletely.
      - F1C2, F2C1, F3C1, F5C1 a few wrap arounds but looks good otherwise, F26C2 ... but not enough ponts, so that makes sense,
    - 2/9 Now back out the extriniscs
    - 2/10 deal with all the null (get cam1-cam2 extriniscs)
    - 2/12 initial a frame wide optimiztion with the per cam per frame results


- Fix distortion wrap around checking in simulator, visuzlizer and calibrator
- get very low error initializing with intrinsics used to generate data
- get low error initializing close, but not exactly the intriniscs used to gerenate data
- gererate synthetic data 3d data with noise
- get reasonable error initializing with intrinsics used to generate data
- get reasonable error initializing close, but not exactly the intriniscs used to gerenate data